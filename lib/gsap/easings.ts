export const EASINGS = {
    luxury: "power3.inOut",    // Slow in, slow out — premium feel
    snap: "power4.out",      // Quick snap in
    elastic: "elastic.out(1,0.5)", // Playful bounce (use sparingly)
    cinematic: "expo.inOut",      // Dramatic long ease
};

export const DURATIONS = {
    fast: 0.4,
    medium: 0.8,
    slow: 1.2,
    cinematic: 2.0,
};

export const STAGGER = {
    tight: 0.05,
    medium: 0.12,
    loose: 0.25,
};